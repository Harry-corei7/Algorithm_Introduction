- 给定一个已排序的链表的头`head`，**删除所有重复的元素，使每个元素只出现一次**。
- 返回`已排序的链表`。

- 示例 1：
  - 输入：head = [1,1,2]
  - 输出：[1,2]

<img width="302" height="242" alt="image" src="https://github.com/user-attachments/assets/43424b90-a63e-4cde-bef0-7e37bf655a8c" />

- 示例 2：
  - 输入：head = [1,1,2,3,3]
  - 输出：[1,2,3]

<img width="542" height="222" alt="image" src="https://github.com/user-attachments/assets/15f8dae1-7a68-4f62-97c1-1818b358113d" />

---
`python语法`
```python
# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def deleteDuplicates(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        dummy_head = ListNode(-1)
        dummy_head.next = head
        pre = dummy_head
        cur = head
        while(cur != None and cur.next != None): # 防止空链表，一个节点链表情况
            if(cur.val == cur.next.val):
                while(cur.next != None and cur.val == cur.next.val):
                    cur = cur.next
                pre.next = cur
            else:
                pre = pre.next
                cur = cur.next
        return dummy_head.next
```
