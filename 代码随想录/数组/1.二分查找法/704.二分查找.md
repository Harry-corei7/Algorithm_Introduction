**二分查找法**

`1) 核心元素: left, right, mid (mid是二分法独有, left&right可能属于双指针的左右指针)`

`2) 核心机制: left = mid + 1; right = mid - 1;`

`3) 疑难点: while(left <= right), 使用<=是因为nums[right] == target的情况`

给定一个 n 个元素有序的（升序）整型数组 nums 和一个目标值 target  ，写一个函数搜索 nums 中的 target，如果目标值存在返回下标，否则返回 -1。

示例 1:

输入: nums = [-1,0,3,5,9,12], target = 9
输出: 4
解释: 9 出现在 nums 中并且下标为 4
示例 2:

输入: nums = [-1,0,3,5,9,12], target = 2
输出: -1
解释: 2 不存在 nums 中因此返回 -1

提示：

你可以假设 nums 中的所有元素是不重复的。

---
`java语法`
```java
class Solution {
    public int search(int[] nums, int target) {
        // 二分搜索法条件: 1.有序数组 2.无重复元素
        // 避免target小于nums[0], 或者大于nums[nums.length-1]时多次循环
        if (target < nums[0] || target > nums[nums.length-1]){
            return -1;
        }
        // 二分法主体
        int left = 0, right = nums.length-1;
        while(left <= right){
            int mid = left + ((right - left) >> 1);
            if (nums[mid] == target){
                return mid;
            }
            else if (nums[mid] < target){
                left = mid +1;
            }
            else if (nums[mid] > target){
                right = mid -1;
            }
        }
        // 没有找到目标值
        return -1;
    }
}
```

---

下面我用几个清晰的例子帮你理解 `>>（右移运算符）` 是怎么操作二进制数的：

例子 1：8 >> 1
步骤：
1) 十进制 8 的二进制是：00001000
2) 向右移动 1 位：00000100
3) 新的二进制值是：4

所以：

```java
8 >> 1 = 4
```

---
`python语法`
```python
class Solution(object):
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        # 二分查找法: (1) 必须是有序数组 (2) 必须没有重复值
        # 1.筛选特殊情况
        if(target < nums[0] or target > nums[len(nums)-1]):
            return -1
        # 2.二分法主体
        left = 0
        right = len(nums)
        while(left <= right):
            mid = (right + left)//2
            if(target == nums[mid]):
                return mid
            elif(target < nums[mid]):
                right = mid - 1
            elif(target > nums[mid]):
                left = mid + 1
        return -1
```
