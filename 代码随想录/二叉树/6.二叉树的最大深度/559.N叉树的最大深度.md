给定一个 `N 叉树`，找到其最大深度。

最大深度是指从根节点到最远叶子节点的最长路径上的节点总数。

`N 叉树`输入按层序遍历序列化表示，每组子节点由空值分隔（请参见示例）。

1) 示例 1：

<image src ="https://github.com/user-attachments/assets/09153780-a286-4fdc-bbcb-1f9fbab785b3" width = 400 height = 300>

- 输入：root = [1,null,3,2,4,null,5,6]
- 输出：3

2) 示例 2：

<image src = "https://github.com/user-attachments/assets/4761106b-166b-4fa8-9dc5-8dab355abe1a" width = 300 height = 400>

- 输入：root = [1,null,2,3,4,5,null,null,6,7,null,8,null,9,10,null,null,11,null,12,null,13,null,null,14]
- 输出：5

---

`N叉树Node的构造`

```java
// Definition for a Node.
class Node {
    public int val;
    public List<Node> children;

    public Node() {}

    public Node(int _val) {
        val = _val;
    }

    public Node(int _val, List<Node> _children) {
        val = _val;
        children = _children;
    }
};
```

---

`N叉树的最大深度`

```java
class Solution {
    public int maxDepth(Node root) {
        Queue<Node> que = new LinkedList<>();
        if(root == null)return 0;
        int nodeNum = 0;
        que.offer(root);
        while(!que.isEmpty()){
            int LevelSize = que.size();
            // 层序遍历算法，LevelSize一层一层遍历
            for(int i = 0; i < LevelSize; i++){
                Node node = que.poll();
                for(int j = 0; j < node.children.size(); j++){
                    que.offer(node.children.get(j));
                }
            }
            nodeNum++;
        }
        return nodeNum;
    }
}
```
