`难度：中等`

- 在一条环路上有`n`个加油站，其中第`i`个加油站有汽油`gas[i]`升。
- 你有一辆油箱容量无限的的汽车，从第`i`个加油站开往第`i+1`个加油站需要消耗汽油`cost[i]`升。
- 你从其中的一个加油站出发，开始时油箱为空。
- 给定两个整数数组`gas`和`cost`，如果你可以按顺序绕环路行驶一周，则返回出发时加油站的编号，否则返回`-1`。
- 温馨提示：如果存在解，则**题目保证**它是`唯一`的。

- 示例 1:
  - 输入: gas = [1,2,3,4,5], cost = [3,4,5,1,2]
  - 输出: 3
  - 解释:
    - 从 3 号加油站(索引为 3 处)出发，可获得 4 升汽油。此时油箱有 = 0 + 4 = 4 升汽油
    - 开往 4 号加油站，此时油箱有 4 - 1 + 5 = 8 升汽油
    - 开往 0 号加油站，此时油箱有 8 - 2 + 1 = 7 升汽油
    - 开往 1 号加油站，此时油箱有 7 - 3 + 2 = 6 升汽油
    - 开往 2 号加油站，此时油箱有 6 - 4 + 3 = 5 升汽油
    - 开往 3 号加油站，你需要消耗 5 升汽油，正好足够你返回到 3 号加油站。
    - 因此，3 可为起始索引。

- 示例 2:
  - 输入: gas = [2,3,4], cost = [3,4,3]
  - 输出: -1
  - 解释:
    - 你不能从 0 号或 1 号加油站出发，因为没有足够的汽油可以让你行驶到下一个加油站。
    - 我们从 2 号加油站出发，可以获得 4 升汽油。 此时油箱有 = 0 + 4 = 4 升汽油
    - 开往 0 号加油站，此时油箱有 4 - 3 + 2 = 3 升汽油
    - 开往 1 号加油站，此时油箱有 3 - 3 + 3 = 3 升汽油
    - 你无法返回 2 号加油站，因为返程需要消耗 4 升汽油，但是你的油箱只有 3 升汽油。
    - 因此，无论怎样，你都不可能绕环路行驶一周。

---
### 贪心算法思路

- i从0开始累加rest[i]，和记为curSum，一旦curSum小于零，说明[0, i]区间都不能作为起始位置，因为这个区间选择任何一个位置作为起点，到i这里都会断油
- 那么起始位置从i+1算起，再从0计算curSum。

<img width="400" height="400" alt="image" src="https://github.com/user-attachments/assets/7fe45326-8447-41f7-aa4a-ae6df3eac438" />

- 问(1)：那么为什么一旦[0，i] 区间和为负数，起始位置就可以是i+1呢，i+1后面就不会出现更大的负数？
- 答(1)：如果出现更大的负数，就是更新i，那么起始位置又变成新的i+1了。

- 问(1)：那有没有可能 [0，i] 区间 选某一个作为起点，累加到 i这里 curSum是不会小于零呢？
- 如图：

<img width="350" height="250" alt="image" src="https://github.com/user-attachments/assets/f26220da-b436-4883-898b-9c4d8b540b20" />

- 答(2)：如果`curSum<0`说明`区间和1`+`区间和2`< 0， 那么，假设从上图中的位置开始计数`curSum不会小于0`的话，就是`区间和2`>0。
- 答(2)：`区间和1`+`区间和2` <0，同时`区间和2` >0，只能说明`区间和1` <0，那么就会从假设的箭头初就开始从新选择起始位置了。
- **那么局部最优：当前累加rest[i]的和curSum一旦小于0，起始位置至少要是i+1，因为从i之前开始一定不行。**
- 全局最优：找到可以跑一圈的起始位置。

---
`python语法：贪心算法`
```python
class Solution(object):
    def canCompleteCircuit(self, gas, cost):
        """
        :type gas: List[int]
        :type cost: List[int]
        :rtype: int
        """
        start_index = 0
        current_gas = 0
        total_gas = 0 # 不计算sum(gas)和sum(cost)，而是设置total_gas
        for i in range(0, len(gas)):
            rest = gas[i] - cost[i]
            current_gas += rest
            total_gas += rest
            if(current_gas < 0):
                start_index = i+1 # 重置start_index
                current_gas = 0 # 重置
        # 当遍历结束，发现计算的total_gas < 0，说明sum(gas) < sum(cost)，直接return -1
        if(total_gas < 0): return -1
        return start_index
```

`python语法：贪心算法过度`
```python
class Solution(object):
    def canCompleteCircuit(self, gas, cost):
        """
        :type gas: List[int]
        :type cost: List[int]
        :rtype: int
        """
        if(sum(gas) < sum(cost)):
            return -1
        
        start_index = 0
        current_gas = 0
        for i in range(0, len(gas)):
            rest = gas[i] - cost[i]
            current_gas += rest
            if(current_gas < 0): # 很妙的一步，一旦<0，立刻换下一个startIndex开始
                # 因为sum(gas) > sum(cost)，肯定有一个startIndex
                # for i in range()走完一遍肯定找得到(这里比较难想)
                start_index = i+1 # 重置start_index
                current_gas = 0 # 重置
        return start_index
```

`python语法：个人理解暴力解法(有冗余，时间复杂度n^2)`
```python
class Solution(object):
    def canCompleteCircuit(self, gas, cost):
        """
        :type gas: List[int]
        :type cost: List[int]
        :rtype: int
        """
        if(sum(gas) < sum(cost)):
            return -1 # 优化：总和不够则肯定不够
        
        for i in range(0, len(gas)):
            if(gas[i] >= cost[i]): # 优化：起始点必须够油去下一站
                start = i
                curSum = 0
                cur = start
                while(curSum >= 0): # 冗余：和下面if(curSum >= cost[cur])重复
                    curSum += gas[cur]
                    if(curSum >= cost[cur]):
                        curSum -= cost[cur] # 启动区下一站，减油
                        if(cur == len(gas)-1): # 模拟绕圈
                            cur = 0
                        else:
                            cur += 1 # 模拟绕圈
                        if(cur == start):
                            return start
                    else:
                        break
```
